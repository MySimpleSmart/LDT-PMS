import { Typography } from 'antd'

export default function Courses() {
  return (
    <div style={{ width: '100%' }}>
      <Typography.Title level={3} style={{ marginBottom: 8 }}>Courses</Typography.Title>
      <Typography.Text type="secondary" style={{ display: 'block' }}>
        Courses — add content here.
      </Typography.Text>
    </div>
  )
}
